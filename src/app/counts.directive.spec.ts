import { CountsDirective } from './counts.directive';

describe('CountsDirective', () => {
  it('should create an instance', () => {
    const directive = new CountsDirective();
    expect(directive).toBeTruthy();
  });
});
